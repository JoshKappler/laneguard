# deps / build
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# human swipe corpus is personal biometric-adjacent data — never committed by
# default. A small anonymized sample lives in corpus/sample/ (committed) for CI.
corpus/*
!corpus/sample/
!corpus/README.md

# generated batch results (regenerated by `pnpm batch`)
results/

# local scratch
.research/
.vercel
.env*
