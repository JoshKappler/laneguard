# deps / build
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# human swipe corpus is personal biometric-adjacent data — never committed by
# default. corpus/sample/ is tracked and reserved for a consented anonymized
# slice; it is currently empty (no corpus recorded yet — see corpus/README.md).
corpus/*
!corpus/sample/
!corpus/README.md

# generated batch results (regenerated by `pnpm batch`)
results/

# the working brief is private: it names the pitch strategy and quotes a
# private email reply. It was tracked in this public repo until 2026-08-23 and
# is still reachable in history (commit e1bf700) — scrub with a force-push if
# that matters.
AGENT-BRIEF.md

# local scratch
.research/
.vercel
.env*
